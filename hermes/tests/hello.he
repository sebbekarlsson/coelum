object person = {
    int width = 32;
    int height = 32;

    object animal = {
        string name = "dog";    
    };
};


bool loop = true;

while (loop)
{
    print(person);
    loop = false;
}
